<h1>Behold, the tweets!</h1>

<ul>
  <% @tweets.each do |tweet| %>
    <li><%= tweet.user.username %> tweeted:
    <%= tweet.text %>
    <% if tweet.user = current_user %>
     |
        <%= link_to "Edit", edit_tweet_path(tweet) %>
     |
      <%= link_to "Delete", tweet_path(tweet), :data => { :confirm => "are you sure?",
                                                        :method => :delete  }%>
    <% end %>
    </li>
  <% end %>
</ul>

<%= render 'form' %>
